#include <stdio.h>
#include <math.h>


int main() {
    int x1, y1;
    printf("digite a base:\n");
    scanf("%d", &x1);
    printf("digite o exponte:\n");
    scanf("%d", &y1);
    
    resultado = poten(x1, y1)
    
    printf("o valor é :%d", poten(x1, y1));

    return 0;
}


int poten(x1, y1){
    int b =1 , i, y1, x1;
    double
    for(i = 0; i < y1; i++){
        b *= y1;
    }
    return b; 

}